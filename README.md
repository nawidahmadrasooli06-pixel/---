# Challenge Bot

Render Start Command:
`python main.py`

Required environment variables:
- BOT_TOKEN
- MONGO_URI
- ADMIN_ID

`handlers/__init__.py` is intentionally empty; it only marks `handlers` as a Python package.
